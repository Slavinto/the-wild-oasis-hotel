import { Filter, TableOperations } from "@/ui";

const CabinTableOperations = () => {
    return (
        <TableOperations>
            <Filter />
        </TableOperations>
    );
};

export default CabinTableOperations;
